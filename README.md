# dotfiles

somewhat non-setup specific dotfiles to maximize usefulness (things like zshrc need to vary between work and home)

## config

extra configuration steps

### sketchybar secrets

`security add-generic-password -a "$USER" -s "sketchybar_glucose_key" -w "your-access-key-value"`

`security add-generic-password -a "$USER" -s "sketchybar_glucose_domain" -w "your-domain"`

